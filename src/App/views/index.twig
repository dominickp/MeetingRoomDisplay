{% extends 'layout.twig' %}
{% block title %}DefaultController::index{% endblock %}

{% block content %}
<h1>Silex Bootstrap</h1>
<p>You have successfully bootstrapped Silex. Now get to work!</p>

    {#{{ dump(events) }}#}

    {% for event in events %}
        Event: {{ event.getSummary }} - {% if event.getStart %}{{ event.getStart.getDateTime }}{% endif %}
        <hr>
    {% endfor %}

{% endblock %}
